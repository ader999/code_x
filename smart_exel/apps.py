from django.apps import AppConfig


class SmartExelConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'smart_exel' # Nombre de tu aplicación
    verbose_name = 'Smart Excel App'
